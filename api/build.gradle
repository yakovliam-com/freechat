plugins {
    id 'java'
    id 'com.github.johnrengelman.shadow'
}

dependencies {
    implementation 'net.kyori:adventure-api:4.10.1'
}

shadowJar {
    archiveBaseName.set("freechat-api-${project.version}")
    archiveClassifier.set('')
    archiveVersion.set('')
}